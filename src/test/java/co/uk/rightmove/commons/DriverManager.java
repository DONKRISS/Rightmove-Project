package co.uk.rightmove.commons;

import org.openqa.selenium.WebDriver;

public class DriverManager {
    public static WebDriver driver; //help us to hold driver(selenium) so that we do not have to create it many times

}
